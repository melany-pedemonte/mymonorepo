version: 1
applications:
 - frontend:
    phases:
      preBuild:
        commands:
          - npm install
      build:
        commands:
          - npm run build:frontend
    artifacts:
      baseDirectory: dist/packages/frontend
      files:
        - '**/*'
    cache:
      paths:
        - node_modules/**/*
   appRoot: /