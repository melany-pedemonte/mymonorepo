version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm install
    build:
      commands:
        - npm run build:frontend
  artifacts:
    # IMPORTANT - Please verify your build output directory
    baseDirectory: dist/packages/frontend
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
